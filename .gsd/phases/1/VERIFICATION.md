## Phase 1 Verification

### Must-Haves
- [x] Project skeleton, clean setup, and URL handling — VERIFIED (evidence: The basic directory structure is created. Normalizer module accurately parses URLs. FastAPI `main.py` is serving a `/health` and `/scan` route. The `pytest` test suite passes successfully without warnings.)

### Verdict: PASS
