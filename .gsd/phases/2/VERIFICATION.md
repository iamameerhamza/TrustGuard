## Phase 2 Verification

### Must-Haves
- [x] Working feature extractor (lexical, keyword, entropy) with tests — VERIFIED (evidence: Extractor successfully processes normalized data, calculates entropy/subdomains/keywords, and outputs it via the `/scan` API; pytest suite passes for both `test_extractor.py` and `test_api.py`)

### Verdict: PASS
