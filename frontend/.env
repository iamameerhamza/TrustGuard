# TrustGuard frontend env vars
# Copy to .env.local and fill in values — never commit this file

# URL of the FastAPI backend
VITE_API_BASE=http://127.0.0.1:8000

# Must match TRUSTGUARD_API_KEY set on the backend
VITE_API_KEY=change_me_to_a_strong_random_secret
